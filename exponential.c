#include<stdio.h>
int main()
{
    double x,result;
    printf("enter the value of x :");
    scanf("%lf",&x);
    result=exp(x);
    printf("the exponential value =%lf",result);







    return 0;
}
